# neuro_video_reader
NeuroVideoCore is a lightweight modular engine for reading, decoding and seeking video frames from files or streams. Provides unified API, deterministic frame access, optional async mode, and pluggable backends (OpenCV, PyAV, GPU). 
